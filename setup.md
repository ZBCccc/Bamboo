我们的实验平台为 AMD Ryzen 9 9950X CPU @ 4.3GHz、186GB 内存、ZHITAI TiPlus7100 4TB NVMe SSD，操作系统为 Ubuntu 22.04.5 LTS。我们使用 CMake (>=3.10) 组织构建，采用 C++17 开发并以 Clang (版本 14.0.0) 进行编译。密码学原语由 OpenSSL 3.0.2 提供：在 Bamboo 的 SSE 组件中使用 AES-128 的 CBC 模式实现对称加密；通过 SHA-256、SHA-384、SHA-512 与 SHA-224 实现哈希与到椭圆曲线点的映射。群运算采用 RELIC 库在 NIST P-256 曲线上实现，同时链接 GMP 与 pthread 作为依赖。存储方面，Bamboo 客户端状态使用 SQLite3；Bamboo 服务器端将密文数据库（CDB）持久化在 PostgreSQL（libpqxx/libpq）；Poseidon 方案默认使用内存型存储维护 T 与 X 结构（其中 XSET 以内存集合实现），并支持数据批量保存与加载。
