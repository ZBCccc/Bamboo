# Pi 函数重新设计文档

## 概述

重新设计了 `pi` 和 `pi_inv` 函数，用于实现字符串到椭圆曲线点的安全双向映射。

## 设计原理

### 数据格式
椭圆曲线点使用 NIST P256 曲线的压缩格式，占用 33 字节：
```
[4字节随机数据] [1字节长度] [字符串数据] [剩余字节填充]
```

### 映射策略
1. **随机化**：使用前4字节作为随机数据，确保相同字符串映射到不同的点
2. **长度编码**：第5字节存储字符串长度（0-28）
3. **数据嵌入**：从第6字节开始存储实际字符串数据
4. **验证循环**：重复尝试直到找到有效的椭圆曲线点

## 函数接口

### pi(ep_t out, const string &in)
将字符串映射到椭圆曲线点。

**参数：**
- `out`: 输出的椭圆曲线点
- `in`: 输入字符串（最大28字节）

**行为：**
- 空字符串：设置 `out` 为无穷远点并输出错误
- 超长字符串：设置 `out` 为无穷远点并输出错误
- 有效字符串：映射到有效的椭圆曲线点

### pi_inv(string &out, ep_t in)
从椭圆曲线点恢复字符串。

**参数：**
- `out`: 输出字符串
- `in`: 输入的椭圆曲线点

**行为：**
- 无穷远点：清空 `out` 并输出错误
- 有效点：提取并返回原始字符串

## 安全特性

1. **输入验证**：严格检查输入参数的有效性
2. **边界检查**：防止缓冲区溢出
3. **错误处理**：优雅处理各种异常情况
4. **循环保护**：防止无限循环（最大1000次尝试）
5. **内存安全**：正确管理临时变量的生命周期

## 性能特点

1. **随机化开销**：每次映射需要多次随机数生成
2. **验证成本**：需要重复验证椭圆曲线点的有效性
3. **内存使用**：使用固定大小的栈缓冲区，无动态分配

## 限制

1. **字符串长度**：最大支持28字节的字符串
2. **性能**：由于随机化过程，性能不如确定性映射
3. **一致性**：同一字符串每次映射的结果不同（这是设计特性）

## 测试结果

- ✅ 基本字符串映射和恢复
- ✅ 边界情况处理（空字符串、超长字符串）
- ✅ 最大长度字符串处理
- ✅ UTF-8 字符串支持
- ✅ 错误处理和验证

## 使用示例

```cpp
#include "primitive.h"

// 初始化 RELIC 库
core_init();
ep_param_set(NIST_P256);

ep_t point;
ep_new(point);

// 字符串到点的映射
string original = "hello world";
pi(point, original);

// 点到字符串的恢复
string recovered;
pi_inv(recovered, point);

// 验证结果
assert(original == recovered);

// 清理
ep_free(point);
core_clean();
```

## 改进建议

1. **性能优化**：可以考虑使用确定性的哈希到曲线映射方法
2. **长度扩展**：如果需要支持更长字符串，可以使用多点映射
3. **压缩优化**：可以使用更高效的数据压缩算法
4. **并行化**：对于批量操作，可以考虑并行处理

## 兼容性

重新设计的函数与原有的调用接口完全兼容，可以直接替换使用。
